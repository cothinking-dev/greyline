result
result-*
__pycache__/
*.py[cod]
.direnv/
.pytest_cache/
dist/
build/
*.egg-info/
.agent-shell/

# Upstream source downloads kept locally for reference, not part of the repo.
*.zip

# IBM/Lenovo ThinkPad artwork — copyright IBM/Lenovo, NOT redistributable (see NOTICE).
# Kept on disk for local/personal use but never committed to this repo.
worldtime/assets/world.time.1400x1050.png
worldtime/assets/thinkpad_logo.png

# local copy of geodata for the demo (the Pages workflow copies it at deploy)
web/data/
